[tox]
requires =
    tox>=4
envlist = py{310,313}

[gh-actions]
python =
    3.10: py310
    3.13: py313

[testenv]
description = run unit tests
allowlist_externals = poetry
commands =
    poetry install -v
    poetry run pytest {posargs:tests}
